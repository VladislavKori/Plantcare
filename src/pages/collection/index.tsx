import { FC } from "react";
import { User } from "@features/collection/ui";

export const CollectionPage: FC = () => {
    return (
        <div>
            Collection
            <User />
        </div>
    )
}